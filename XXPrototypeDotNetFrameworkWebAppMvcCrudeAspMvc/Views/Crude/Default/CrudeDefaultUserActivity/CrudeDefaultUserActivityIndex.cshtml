<!--
  SQL2X Generated code based on a SQL Server Schema
  SQL2X Version: 1.0
  http://sql2x.org/
  Generated Date: 4/22/2020 6:56:00 AM
  Template: sql2x.TemplateFromCrudeMvcGenerator.FromCrudeViewIndex
-->
@model IEnumerable<SolutionNorSolutionPim.BusinessLogicLayer.CrudeDefaultUserActivityContract>

@{
    ViewBag.Title = "Index Default User Activity";
}

<h2>Index for Default User Activity</h2>

<p>
    @Html.ActionLink("Create New", "CrudeDefaultUserActivityCreate")
</p>

<table class="table">
    <tr>
        <th>Default User Activity Type</th>
        <th>User Activity Note</th>
        <th>Originating Address</th>
        <th>Referrer</th>
        <th>Date Time</th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.DefaultUserActivityTypeRcd) </td>
            <td>@Html.DisplayFor(modelItem => item.UserActivityNote) </td>
            <td>@Html.DisplayFor(modelItem => item.OriginatingAddress) </td>
            <td>@Html.DisplayFor(modelItem => item.Referrer) </td>
            <td>@Html.DisplayFor(modelItem => item.DateTime) </td>
            <td>
                @Html.ActionLink("Edit", "CrudeDefaultUserActivityEdit", new { defaultUserActivityId = item.DefaultUserActivityId })
                | @Html.ActionLink("Delete", "CrudeDefaultUserActivityDelete", new { defaultUserActivityId = item.DefaultUserActivityId })
            </td>
        </tr>
    }

</table>
