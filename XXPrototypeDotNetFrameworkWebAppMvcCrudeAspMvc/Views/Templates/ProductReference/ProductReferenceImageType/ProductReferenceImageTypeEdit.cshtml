<!--
  SQL2X Generated code based on a SQL Server Schema
  SQL2X Version: 1.0
  http://sql2x.org/
  Generated Date: 4/27/2020 3:59:42 PM
  Template: sql2x.TemplateByServiceTableCrudGenerator.ParentViewEdit
-->
@model SolutionNorSolutionPim.BusinessLogicLayer.ProductReferenceImageTypeContract

@{
    ViewBag.Title = "Edit Product Image Type Ref";
}

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ChecksumAfterGet)

        @for (int i = 0; i < @Model.ProductImageTypeRef.Count; i++) {
            @Html.HiddenFor(model => model.ProductImageTypeRef[i].UserId)
            @Html.HiddenFor(model => model.ProductImageTypeRef[i].DateTime)
        }

    </div>
    <h3>Product Image Type Ref</h3>
    <div>
        @Html.ActionLink("references", "DashboardPIMReference", "Dashboard") | 
        @Html.ActionLink("refresh", "ProductReferenceImageTypeEdit", "ProductReferenceImageType") | 
        @Html.ActionLink("add new", "ProductReferenceImageTypeCreate", "ProductReferenceImageType")
    </div>
    <hr />
    <table class="table">
        <tr>
            <th>Product Image Type</th>
            <th>Product Image Type Name</th>
            <th></th>
        </tr>

        @for (int i = 0; i < @Model.ProductImageTypeRef.Count; i++) {
            <tr>
                <td>@Html.EditorFor(modelItem => Model.ProductImageTypeRef[i].ProductImageTypeRcd, new { htmlAttributes = new { @class = "form-control" } })</td>
                <td>@Html.EditorFor(modelItem => Model.ProductImageTypeRef[i].ProductImageTypeName, new { htmlAttributes = new { @class = "form-control" } })</td>
            </tr>
        }

    </table>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Styles.Render("~/Content/themes/base/all.css")
    <script type="text/javascript">
        $(function () {
            // This will make every element with the class "date-picker" into a DatePicker element
            $('.date-picker').datepicker(({ dateFormat: "yy-mm-dd" }));
        })
    </script>
}
