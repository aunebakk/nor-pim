@model IEnumerable<norpim.BusinessLogicLayer.CrudeProductImageTypeRefContract>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CrudeProductImageTypeRefCreate")
</p>

<table class="table">
    <tr>
        <th>Product Image Type</th>
        <th>Product Image Type Name</th>
        <th>Date Time</th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.ProductImageTypeRcd) </td>
            <td>@Html.DisplayFor(modelItem => item.ProductImageTypeName) </td>
            <td>@Html.DisplayFor(modelItem => item.DateTime) </td>
            <td>@Html.ActionLink("Details", "CrudeProductImageTypeRefDetails", new { productImageTypeRcd = item.ProductImageTypeRcd }) |
                @Html.ActionLink("Edit", "CrudeProductImageTypeRefEdit", new { productImageTypeRcd = item.ProductImageTypeRcd }) |
                @Html.ActionLink("New", "CrudeProductImageTypeRefCreate", new { })
            </td>
        </tr>
    }

</table>
