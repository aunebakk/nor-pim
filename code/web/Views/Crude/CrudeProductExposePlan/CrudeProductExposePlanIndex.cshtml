@model IEnumerable<norpim.BusinessLogicLayer.CrudeProductExposePlanContract>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CrudeProductExposePlanCreate")
</p>

<table class="table">
    <tr>
        <th>Product Expose Set Type</th>
        <th>Date Time</th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.ProductExposeSetTypeRcd) </td>
            <td>@Html.DisplayFor(modelItem => item.DateTime) </td>
            <td>@Html.ActionLink("Details", "CrudeProductExposePlanDetails", new { productExposePlanId = item.ProductExposePlanId }) |
                @Html.ActionLink("Edit", "CrudeProductExposePlanEdit", new { productExposePlanId = item.ProductExposePlanId }) |
                @Html.ActionLink("New", "CrudeProductExposePlanCreate", new { })
            </td>
        </tr>
    }

</table>
