@model IEnumerable<norpim.BusinessLogicLayer.CrudeProductChangeSetContract>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CrudeProductChangeSetCreate")
</p>

<table class="table">
    <tr>
        <th>Date Time</th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.DateTime) </td>
            <td>@Html.ActionLink("Details", "CrudeProductChangeSetDetails", new { productChangeSetId = item.ProductChangeSetId }) |
                @Html.ActionLink("Edit", "CrudeProductChangeSetEdit", new { productChangeSetId = item.ProductChangeSetId }) |
                @Html.ActionLink("New", "CrudeProductChangeSetCreate", new { })
            </td>
        </tr>
    }

</table>
