@model IEnumerable<norpim.BusinessLogicLayer.CrudeProductInfoContract>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CrudeProductInfoCreate")
</p>

<table class="table">
    <tr>
        <th>Product Info</th>
        <th>Product Info Value</th>
        <th>Date Time</th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.ProductInfoRcd) </td>
            <td>@Html.DisplayFor(modelItem => item.ProductInfoValue) </td>
            <td>@Html.DisplayFor(modelItem => item.DateTime) </td>
            <td>@Html.ActionLink("Details", "CrudeProductInfoDetails", new { productInfoId = item.ProductInfoId }) |
                @Html.ActionLink("Edit", "CrudeProductInfoEdit", new { productInfoId = item.ProductInfoId }) |
                @Html.ActionLink("New", "CrudeProductInfoCreate", new { })
            </td>
        </tr>
    }

</table>
