@model IEnumerable<norpim.BusinessLogicLayer.CrudeProductExposedSelectionContract>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CrudeProductExposedSelectionCreate")
</p>

<table class="table">
    <tr>
        <th>Date Time</th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.DateTime) </td>
            <td>@Html.ActionLink("Details", "CrudeProductExposedSelectionDetails", new { productExposedSelectionId = item.ProductExposedSelectionId }) |
                @Html.ActionLink("Edit", "CrudeProductExposedSelectionEdit", new { productExposedSelectionId = item.ProductExposedSelectionId }) |
                @Html.ActionLink("New", "CrudeProductExposedSelectionCreate", new { })
            </td>
        </tr>
    }

</table>
