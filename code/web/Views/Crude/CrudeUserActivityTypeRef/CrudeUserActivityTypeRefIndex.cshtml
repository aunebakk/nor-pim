@model IEnumerable<norpim.BusinessLogicLayer.CrudeUserActivityTypeRefContract>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CrudeUserActivityTypeRefCreate")
</p>

<table class="table">
    <tr>
        <th>User Activity Type</th>
        <th>User Activity Type Name</th>
        <th>Date Time</th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.UserActivityTypeRcd) </td>
            <td>@Html.DisplayFor(modelItem => item.UserActivityTypeName) </td>
            <td>@Html.DisplayFor(modelItem => item.DateTime) </td>
            <td>@Html.ActionLink("Details", "CrudeUserActivityTypeRefDetails", new { userActivityTypeRcd = item.UserActivityTypeRcd }) |
                @Html.ActionLink("Edit", "CrudeUserActivityTypeRefEdit", new { userActivityTypeRcd = item.UserActivityTypeRcd }) |
                @Html.ActionLink("New", "CrudeUserActivityTypeRefCreate", new { })
            </td>
        </tr>
    }

</table>
