<!--
  SQL2X Generated code based on a SQL Server Schema
  SQL2X Version: 1.0
  http://sql2x.org/
  Generated Date: 12/30/2020 12:16:39 PM
  From Machine: DESKTOP-LSRVP12
  Template: sql2x.TemplateByServiceTableCrudGenerator.ParentViewEdit
-->
@model SolutionNorSolutionPim.BusinessLogicLayer.ProductReferenceImageTypeContract

@{
    ViewBag.Title = "Edit Product Info Ref";
}

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ChecksumAfterGet)

        @for (int i = 0; i < @Model.ProductInfoRef.Count; i++) {
            @Html.HiddenFor(model => model.ProductInfoRef[i].ProductInfoRcd)
            @Html.HiddenFor(model => model.ProductInfoRef[i].UserId)
            @Html.HiddenFor(model => model.ProductInfoRef[i].DateTime)
        }

    </div>
    <h3>Product Info Ref</h3>
    <div>
        @Html.ActionLink("references", "DashboardPIMReference", "Dashboard") | 
        @Html.ActionLink("refresh", "ProductReferenceInfoEdit", "ProductReferenceInfo") | 
        @Html.ActionLink("add new", "ProductReferenceInfoCreate", "ProductReferenceInfo")
    </div>
    <hr />
    <table class="table">
        <tr>
            <th>Product Info</th>
            <th>Product Info Name</th>
            <th></th>
        </tr>

        @for (int i = 0; i < @Model.ProductInfoRef.Count; i++) {
            <tr>
                <td>@Html.EditorFor(modelItem => Model.ProductInfoRef[i].ProductInfoRcd, new { htmlAttributes = new { @class = "form-control" } })</td>
                <td>@Html.EditorFor(modelItem => Model.ProductInfoRef[i].ProductInfoName, new { htmlAttributes = new { @class = "form-control" } })</td>
            </tr>
        }

    </table>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn-primary" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Styles.Render("~/Content/themes/base/all.css")
    <script type="text/javascript">
        $(function () {
            // This will make every element with the class "date-picker" into a DatePicker element
            $('.date-picker').datepicker(({ dateFormat: "yy-mm-dd" }));
        })
    </script>
}
